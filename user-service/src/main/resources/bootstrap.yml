spring:
  application:
    name: userservice # 服务名称
  profiles:
    active: dev # 环境，这里是dev
  cloud:
    nacos:
      server-addr: localhost:8848 # Nacos地址 单机8848 Nginx80
      config:
        file-extension: yaml # 文件后缀名